import styled from "styled-components";
import tw from "twin.macro";

export const StyledAddableLevelValuesInputsWrapper = styled.div`
    ${tw`flex items-center justify-center gap-2 max-w-[150px]`}
`;
