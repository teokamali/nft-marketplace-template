import { defineMessages } from "react-intl";

export const scope = "app.HomePage";

export default defineMessages({
    TITLE: {
        id: `${scope}.TITLE`,
        defaultMessage: "Top collections over",
    },
    SUBTITLE: {
        id: `${scope}.SUBTITLE`,
        defaultMessage: "Collecting digital art is the best hobby ever.",
    },
});
