import { defineMessages } from "react-intl";

export const scope = "app.productPage";

export default defineMessages({
    Notable_Drops: {
        id: `${scope}.Notable_Drops`,
        defaultMessage: "Notable Drops",
    },
});
