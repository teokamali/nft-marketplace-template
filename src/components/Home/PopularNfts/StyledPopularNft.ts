import styled from "styled-components";
import tw from "twin.macro";

export const PopularNftsWrapper = styled.div`
    ${tw`pt-28`}
`;

export const PopularNftsTitle = styled.h3`
    ${tw`text-center pb-5 text-[31px] font-semibold text-black dark:text-smokeWhite`}
`;
export const MobilePopularNftsWrapper = styled.div`
    ${tw` max-w-[285.7px] mx-auto flex flex-col items-center gap-6`}
`;
